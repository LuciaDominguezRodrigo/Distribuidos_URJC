services:
  web:
    image: luciadominguezrodrigo/urbanthreads:latest
    #build:
    #  context: .
    #  dockerfile: Dockerfile
    ports:
      - 8080:8080
    environment:
      - SPRING_DATASOURCE_URL=jdbc:mysql://db:3306/urbanthreads?serverTimezone=UTC
      - SPRING_DATASOURCE_USERNAME=root
      - SPRING_DATASOURCE_PASSWORD=password
    restart: on-failure
  db:
    image: mysql:8.0.36
    restart: always
    environment:
      - MYSQL_ROOT_PASSWORD=password
      - MYSQL_DATABASE=urbanthreads
    volumes:
      - ./mysql:/var/lib/mysql


